# Activity-01 – Recursive Directory Scanner with Enhancement

## 🔧 Enhancement Chosen
**`--min-size` filter**  
This optional argument allows the script to only display `.txt` files that are larger than the specified size in **kilobytes**.

## 🏁 How to Run

### 1. Generate test folders and files
This command runs the setup script and creates a directory structure under `test_root/`:

```bash
python3 setup_files.py
